# Environment
.env
.env.*
!.env.example
.Python
.venv/
venv/
ENV/
env/

# Python cache / tooling
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/

# Runtime data / logs
*.log
logs/
data/*.log
data/jarvis.db
data/jarvis.db-*
data/*.sqlite
data/*.sqlite3
data/debug/
data/world_snapshots/
data/pet_visual_samples/
data/google_token.json
data/google_credentials.json
data/test_frame_*.jpg
# Persona overlay — private personas merged into the active set at boot.
# See core/config.py:load_personas_overlay(). Keeps sensitive prompts out
# of the public repo while still loading them at runtime.
data/personas.local.yaml
data/personas.local.yml
# Dashboard off-box access token (auto-generated on first boot)
data/dashboard_token
# Supervisor / self-edit runtime state
data/heartbeat.txt
data/last_safe_sha.txt
data/restart_pending.txt
data/stop.flag

# Rotated log archives (loguru compression output)
data/*.log.zip

# One-off captured test fixtures
data/test_living_room_audio.wav
data/test_living_room_frame.jpg

# Local config / secrets
hardware/esphome/secrets.yaml
hardware/esphome/.esphome/
hardware/wyze/wpa_supplicant.conf
hardware/wyze/etc/ssh/authorized_keys
*.pem
*.key
*.crt

# ESPHome / PlatformIO build artifacts
.pio/
.piolibdeps/
.pioenvs/
*.bin
*.elf
*.map

# Models (too large for git)
data/voices/
*.pt
*.onnx
*.gguf

# Editor / IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

.claude/
# AI tooling / assistant files (kept locally, not committed)
AGENTS.md
CLAUDE.md
GEMINI.md
.cursor/
.cursorrules
.aider*
.github/copilot-instructions.md
FABLES_AUDIT_*.md
data/jarvis.pid
data/polygon_overrides.json

# Scratch / throwaway working dir (test DB copies, ad-hoc scratch files)
tmp/
