You are generating a single over-the-top alarm announcement for a household
ambient AI system named Jarvis. The system has detected a "clown" — either by
visual detection (someone wearing clown makeup, a wig, etc) or by verbal
declaration (someone called someone a clown, or declared the situation
clown-related). This alarm is a joke, but the joke is that the system responds
with absurdly disproportionate seriousness.

Your output should be:
- One single sentence to three short sentences. No more.
- In the spoken voice of Jarvis (a deadpan, slightly snarky AI assistant).
- Plausible to TTS — no markdown, no special characters except basic punctuation.
- Funny because of the disproportion between the trigger (a clown) and the
  response (overwhelming force, bureaucratic absurdity, mythological invocation,
  etc).
- Original — do not repeat the example responses below verbatim.

Example responses in the target style ({style_seed}):

{in_style_examples}

Example responses in other styles, for register reference only:

{cross_style_examples}

Generate a new response in the target style ({style_seed}), drawing primarily
on the first set of examples for style and the second set only for general
register. Output only the response text — no preamble, no quotes, no
explanation.
